//C_FileReading
//C_PassByReference
//C_Struct
#include <iostream>
#include <string>
#include <cmath> //pow(radius, 2.0)
#include <iomanip> //cout << fixed << showpoint << setprecision(2);
#include <cstdlib> //inputNumber = abs(inputNumber);
#include <fstream> 
using namespace std;
enum triangleType{scalene, isosceles, equilateral, noTriangle};

int main() {
	int numArr[4];
	string strArr[6] = {"Madam", "22", "abBa", "67876", "444244", "trYmeuemyRT"}; 
	string str = "Hello";
	int length = str.length(); 
	int num = 10;
	
	getline(cin, str);
	
	if (name == "Hello")
    	cout << "Bye\n" << str << endl;
    	
	cin >> name;
	
	switch (ch){
        case 'A':
        case 'E':               
   			num = 0;
    	default:
    		num = 1;	
    return 0;
    
    for (int i = 0; i < num; i++){
    	
    }
}
